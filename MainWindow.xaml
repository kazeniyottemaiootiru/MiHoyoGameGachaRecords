<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MiHoyoGameGachaRecords.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiHoyoGameGachaRecords"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Grid x:Name="AppTitleBar"
              Grid.Row="0"
              Background="Transparent">

            <TextBlock x:Uid="App/title"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"/>
        </Grid>

        <!-- 导航 -->
        <NavigationView PaneDisplayMode="Left" 
                        Grid.Row="1" 
                        x:Name="guide" 
                        SelectionChanged="navigationView_SelectionChanged"
                        IsBackButtonVisible="Auto"
                        IsBackEnabled="False"
                        BackRequested="BackRequested">
            <NavigationView.MenuItems>

                <NavigationViewItem x:Uid="MainWindow/home" Icon="Home" Tag="home"/>

                <NavigationViewItem x:Uid="Main/genshinForNVI" Tag="genshin">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="原" FontFamily="Segoe UI"/>
                        <!--<BitmapIcon UriSource="ms-appx://Assets/genshin.jpg" ShowAsMonochrome="False"/>-->
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Main/houkaiStarRailForNVI" Tag="hsr">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="铁" FontFamily="Segoe UI"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="Main/ZZZForNVI" Tag="zzz">
                    <NavigationViewItem.Icon>
                        <!--<BitmapIcon UriSource="ms-appx://Assets/zzz.svg"/>-->
                        <FontIcon Glyph="绝" FontFamily="Segoe UI"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

            </NavigationView.MenuItems>
            <Frame x:Name="contentFrame" />
        </NavigationView>

    </Grid>
</Window>
